# handball-web


```main
git init
git remote add origin
```

# créer une branche

```bash
git checkout -b nouvelle
```

```bash
git add .
git commit -m "Initial commit"
git push -u origin master
```

```bash
git 